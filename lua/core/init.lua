---Module containing all core implementations of the configuration
return {
    base = require("core.base"),
    options = require("core.options"),
    keymaps = require("core.keymaps"),
    autocmds = require("core.autocmds"),
}
