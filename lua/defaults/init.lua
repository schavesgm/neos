---Module containing all neos default tables.
return {
    options = require("defaults.options"),
    keymaps = require("defaults.keymaps"),
    autocmds = require("defaults.autocmds"),
    commands = require("defaults.commands"),
}
